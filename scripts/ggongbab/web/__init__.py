"""Unattended Dooray web agent: mailbox -> candidate detection -> project task.

The agent never analyses events. It only moves candidate mails into the
collection project, where the existing pipeline (refresh_ggongbab.py) takes over.
"""
