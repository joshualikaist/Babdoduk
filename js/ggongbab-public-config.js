/* Browser-public values ONLY. See docs/GGONGBAB_REALTIME.md.
 * Empty configuration deliberately keeps the static JSON feed working.
 * SUPABASE_PUBLISHABLE_KEY may also hold the legacy browser anon key.
 * No environment-file loading or implicit environment-variable expansion.
 */
window.BABDODUK_PUBLIC_FEED_CONFIG = Object.freeze({
  SUPABASE_URL: '',
  SUPABASE_PUBLISHABLE_KEY: ''
});
