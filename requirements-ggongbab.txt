openai>=1.60
pydantic>=2.6
pytest>=8
requests>=2.32,<3

# Only needed for the unattended mailbox agent (scripts/dooray_web_agent.py).
# After installing: python -m playwright install chromium
playwright>=1.45
